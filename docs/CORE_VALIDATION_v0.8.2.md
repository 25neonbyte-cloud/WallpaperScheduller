# Core Validation v0.8.2

Esta build congela o core funcional antes da implementação dos motores de Conforto Visual.

Escopo desta build:
- motor de regras, rotação, multi-monitor, tray, startup, eventos de sistema e hardening já validados;
- UI/UX v0.8.x preservada;
- correção de DPI awareness com PerMonitorV2 via manifesto;
- metadados de versão sincronizados em 0.8.2;
- Conforto Visual permanece apenas como próxima etapa, sem schema, persistência ou backend ativo nesta build.

Objetivo: bateria de testes reais do core antes de adicionar Tema do sistema, Temperatura e tom e Rotina visual.
