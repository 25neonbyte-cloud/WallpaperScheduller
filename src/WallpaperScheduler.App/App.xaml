<Application x:Class="WallpaperScheduler.App.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:WallpaperScheduler.App">
    <Application.Resources>
        <local:EnumDisplayConverter x:Key="EnumDisplayConverter"/>
        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter"/>

        <!-- Base visual system -->
        <LinearGradientBrush x:Key="AppBackgroundBrush" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#F7FAFE" Offset="0"/>
            <GradientStop Color="#F2F6FC" Offset="1"/>
        </LinearGradientBrush>
        <LinearGradientBrush x:Key="SidebarBrush" StartPoint="0,0" EndPoint="0,1">
            <GradientStop Color="#FBFDFF" Offset="0"/>
            <GradientStop Color="#EDF5FF" Offset="1"/>
        </LinearGradientBrush>
        <SolidColorBrush x:Key="SurfaceBrush" Color="#FFFFFFFF"/>
        <SolidColorBrush x:Key="SurfaceMutedBrush" Color="#F8FAFD"/>
        <SolidColorBrush x:Key="SurfaceHoverBrush" Color="#F2F6FC"/>
        <SolidColorBrush x:Key="TextPrimaryBrush" Color="#0D1B3A"/>
        <SolidColorBrush x:Key="TextSecondaryBrush" Color="#60708D"/>
        <SolidColorBrush x:Key="TextMutedBrush" Color="#8794AA"/>
        <SolidColorBrush x:Key="BorderBrush" Color="#DDE6F2"/>
        <SolidColorBrush x:Key="BorderStrongBrush" Color="#C9D6E8"/>

        <!-- Product accent -->
        <SolidColorBrush x:Key="AccentBrush" Color="#1177F4"/>
        <SolidColorBrush x:Key="AccentHoverBrush" Color="#0868E6"/>
        <SolidColorBrush x:Key="AccentSoftBrush" Color="#E9F3FF"/>
        <SolidColorBrush x:Key="AccentSofterBrush" Color="#F4F9FF"/>
        <SolidColorBrush x:Key="SuccessBrush" Color="#149447"/>
        <SolidColorBrush x:Key="SuccessSoftBrush" Color="#EAF8EF"/>
        <SolidColorBrush x:Key="DangerBrush" Color="#DC3F46"/>
        <SolidColorBrush x:Key="DangerSoftBrush" Color="#FFF0F1"/>

        <!-- Functional color families -->
        <LinearGradientBrush x:Key="MonitorGroupBrush" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#F1F8FF" Offset="0"/>
            <GradientStop Color="#E8F3FF" Offset="1"/>
        </LinearGradientBrush>
        <SolidColorBrush x:Key="MonitorGroupBorderBrush" Color="#C9E2FF"/>
        <SolidColorBrush x:Key="MonitorAccentBrush" Color="#1677E8"/>
        <SolidColorBrush x:Key="MonitorIconBrush" Color="#E1F0FF"/>

        <LinearGradientBrush x:Key="ComfortGroupBrush" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#FFFBF2" Offset="0"/>
            <GradientStop Color="#FFF4E2" Offset="1"/>
        </LinearGradientBrush>
        <SolidColorBrush x:Key="ComfortGroupBorderBrush" Color="#F2D59A"/>
        <SolidColorBrush x:Key="ComfortAccentBrush" Color="#D98B00"/>
        <SolidColorBrush x:Key="ComfortIconBrush" Color="#FFF0C8"/>
        <SolidColorBrush x:Key="ComfortCardBrush" Color="#FFFCF6"/>

        <LinearGradientBrush x:Key="ScheduleGroupBrush" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#F8F6FF" Offset="0"/>
            <GradientStop Color="#F1EEFF" Offset="1"/>
        </LinearGradientBrush>
        <SolidColorBrush x:Key="ScheduleGroupBorderBrush" Color="#D9D0FF"/>
        <SolidColorBrush x:Key="ScheduleAccentBrush" Color="#7457E8"/>
        <SolidColorBrush x:Key="ScheduleIconBrush" Color="#ECE7FF"/>

        <!-- Theme-sensitive detail resources -->
        <SolidColorBrush x:Key="MonitorCardBrush" Color="#FFFFFF"/>
        <SolidColorBrush x:Key="MonitorCardBorderBrush" Color="#D9E8F8"/>
        <SolidColorBrush x:Key="ScheduleCardBrush" Color="#FFFFFF"/>
        <SolidColorBrush x:Key="ScheduleCardBorderBrush" Color="#DDD8F4"/>
        <SolidColorBrush x:Key="ScheduleDividerBrush" Color="#ECE8F9"/>
        <SolidColorBrush x:Key="DropZoneBrush" Color="#FFFFFF"/>
        <SolidColorBrush x:Key="DropZoneBorderBrush" Color="#94C4FF"/>
        <SolidColorBrush x:Key="SidebarCalloutBorderBrush" Color="#D6E8FF"/>
        <SolidColorBrush x:Key="ComfortBadgeBrush" Color="#FFF0C8"/>
        <SolidColorBrush x:Key="ComfortStatusBrush" Color="#FFF8E6"/>
        <SolidColorBrush x:Key="DangerBorderBrush" Color="#F6C9CC"/>
        <SolidColorBrush x:Key="ToggleTrackBrush" Color="#DCE4EF"/>
        <SolidColorBrush x:Key="ToggleThumbBrush" Color="#FFFFFF"/>

        <!-- Shadows -->
        <DropShadowEffect x:Key="CardShadow" Color="#1B355A" BlurRadius="18" ShadowDepth="2" Opacity="0.07"/>
        <DropShadowEffect x:Key="SoftShadow" Color="#1B355A" BlurRadius="10" ShadowDepth="1" Opacity="0.045"/>

        <!-- Typography -->
        <Style x:Key="SectionTitleStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="19"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}"/>
        </Style>
        <Style x:Key="SectionSubtitleStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Foreground" Value="{DynamicResource TextSecondaryBrush}"/>
        </Style>
        <Style x:Key="FieldLabelStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="{DynamicResource TextSecondaryBrush}"/>
        </Style>
        <Style x:Key="IconTextStyle" TargetType="TextBlock">
            <Setter Property="FontFamily" Value="Segoe Fluent Icons"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
        </Style>

        <!-- Cards -->
        <Style x:Key="CardBorderStyle" TargetType="Border">
            <Setter Property="Background" Value="{DynamicResource SurfaceBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="Padding" Value="16"/>
            <Setter Property="Effect" Value="{StaticResource SoftShadow}"/>
        </Style>

        <!-- Buttons -->
        <Style x:Key="BaseButtonStyle" TargetType="Button">
            <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}"/>
            <Setter Property="Background" Value="{DynamicResource SurfaceBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="14,8"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Root"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="9"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Root" Property="Opacity" Value="0.91"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Root" Property="Opacity" Value="0.78"/>
                            </Trigger>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter TargetName="Root" Property="BorderBrush" Value="{DynamicResource AccentBrush}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Root" Property="Opacity" Value="0.45"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="PrimaryButtonStyle" TargetType="Button" BasedOn="{StaticResource BaseButtonStyle}">
            <Setter Property="Background" Value="{DynamicResource AccentBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource AccentBrush}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="16,9"/>
        </Style>

        <Style x:Key="SecondaryButtonStyle" TargetType="Button" BasedOn="{StaticResource BaseButtonStyle}">
            <Setter Property="Background" Value="{DynamicResource SurfaceBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
        </Style>

        <Style x:Key="NavButtonStyle" TargetType="Button" BasedOn="{StaticResource BaseButtonStyle}">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Padding" Value="12,10"/>
            <Setter Property="Margin" Value="0,2"/>
            <Setter Property="FontSize" Value="13"/>
        </Style>

        <Style x:Key="NavButtonActiveStyle" TargetType="Button" BasedOn="{StaticResource NavButtonStyle}">
            <Setter Property="Background" Value="{DynamicResource AccentSoftBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource SidebarCalloutBorderBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource AccentBrush}"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
        </Style>

        <Style x:Key="SegmentButtonStyle" TargetType="Button" BasedOn="{StaticResource BaseButtonStyle}">
            <Setter Property="Padding" Value="16,7"/>
            <Setter Property="Background" Value="{DynamicResource SurfaceMutedBrush}"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="FontSize" Value="12"/>
        </Style>

        <Style x:Key="IconButtonStyle" TargetType="Button" BasedOn="{StaticResource BaseButtonStyle}">
            <Setter Property="Padding" Value="8"/>
            <Setter Property="MinWidth" Value="34"/>
            <Setter Property="MinHeight" Value="34"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
        </Style>

        <!-- Toggle switch -->
        <Style x:Key="ToggleSwitchStyle" TargetType="CheckBox">
            <Setter Property="Width" Value="42"/>
            <Setter Property="Height" Value="24"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <Border x:Name="Track" Background="{DynamicResource ToggleTrackBrush}" CornerRadius="12" Width="42" Height="24">
                            <Grid>
                                <Ellipse x:Name="Thumb" Width="18" Height="18" Fill="{DynamicResource ToggleThumbBrush}" HorizontalAlignment="Left" Margin="3,0,0,0">
                                    <Ellipse.Effect><DropShadowEffect BlurRadius="4" ShadowDepth="1" Opacity="0.18"/></Ellipse.Effect>
                                </Ellipse>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="Track" Property="Background" Value="{DynamicResource AccentBrush}"/>
                                <Setter TargetName="Thumb" Property="HorizontalAlignment" Value="Right"/>
                                <Setter TargetName="Thumb" Property="Margin" Value="0,0,3,0"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Track" Property="Opacity" Value="0.45"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Inputs -->
        <Style TargetType="TextBox">
            <Setter Property="Padding" Value="10,7"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="{DynamicResource SurfaceBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}"/>
            <Setter Property="CaretBrush" Value="{DynamicResource TextPrimaryBrush}"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="InputBorder" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="7">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter TargetName="InputBorder" Property="BorderBrush" Value="{DynamicResource AccentBrush}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="InputBorder" Property="Opacity" Value="0.55"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ComboBox">
            <Setter Property="Padding" Value="9,6"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
            <Setter Property="Background" Value="{DynamicResource SurfaceBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}"/>
            <Setter Property="MinHeight" Value="34"/>
        </Style>

        <Style TargetType="CheckBox">
            <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
    </Application.Resources>
</Application>